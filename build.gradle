plugins {
    id 'java'
    id 'application' // Если генерировали как Application
}

group 'ru.netology'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.testng:testng:7.1.0'

}

test {
    useTestNG()
}
// build.gradle (только измененные/добавленные части)
dependencies {
    testImplementation 'junit:junit:4.13' // Добавлено
}

test {
    useJUnit() // Добавлено
    testLogging {
        events "passed", "skipped", "failed"
    }
}
